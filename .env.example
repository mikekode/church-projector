# Environment Variables Template
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to git!

# ================================
# CREENLY - Secure API Proxy
# ================================
# After deploying api-proxy/ to Vercel, set your deployment URL here:
NEXT_PUBLIC_API_PROXY_URL=https://your-proxy.vercel.app
API_PROXY_URL=https://your-proxy.vercel.app

# ================================
# LOCAL DEV KEYS (remove after proxy deployment)
# ================================
# These keys are only for local development.
# In production, keys should ONLY be in the Vercel proxy environment.

# OpenAI API Key (move to Vercel proxy env vars)
OPENAI_API_KEY=sk-xxx

# Deepgram API Key (move to Vercel proxy env vars)
NEXT_PUBLIC_DEEPGRAM_API_KEY=xxx
